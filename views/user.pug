extends layout

block content
  br
  .w3-container
    .w3-card-4.w3-center(style="padding: 15px;")
      img(src=user.picture)
      p Welcome #{user.nickname}
      a(href="/user/"+user.nickname class="w3-button w3-ripple w3-red") Your Pins